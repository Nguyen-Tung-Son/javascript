const name = "Tung Son";
const age = 23;

export {name, age};

const info = () => {
    const name = "Khanh";
    const age = 20;
    return name + ' is ' + age + 'years old.';
    };
    
    export default info;